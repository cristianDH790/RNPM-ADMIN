import { Estado } from "./estado";

export class NoticiaCategoria {
    idNoticiaCategoria!: number;
    estado!: Estado;
    nombre!: string;
    urlAmigable!: string;
    orden!: number;
    fecha!: Date;

}
